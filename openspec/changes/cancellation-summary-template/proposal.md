# Proposal: Consolidated cancellation summary notification

Replace the legacy diagnosis-fee cancellation template with one utility template per cancellation operation. The notification carries a PDF that consolidates every selected item, its cancellation reason, diagnostic state and applicable amount.

Rollback: restore the legacy template builder and remove the post-commit summary preparation from the item cancellation service.
